import { db } from '@/lib/db';
export default async function Media() { const media = await db.mediaAsset.findMany({ orderBy: { createdAt: 'desc' }, take: 50 }); return <><div className="page-heading"><div><span className="eyebrow">ARQUIVOS</span><h1>Biblioteca de mídia.</h1><p>Imagens enviadas para as peças e logos oficiais cadastradas.</p></div></div>{media.length ? <div className="media-grid">{media.map(item => <div className="media-item" key={item.id}><img src={item.url} alt={item.name} /><span>{item.name}</span></div>)}</div> : <div className="empty-state"><h3>Sem arquivos por enquanto.</h3><p>Envie uma logo em Perfis ou uma foto no editor.</p></div>}</>; }
